-- ============================================================
-- 智面 · Java 面试题导入(来源: Java面试题与标准回答汇总.md,28 题)
-- 题目 id 101-128,沿用 test-data.sql 的分类/标签体系
-- 可重复执行:先清掉这批 id 再插入,不影响 1-66 的种子题目
-- ============================================================

USE zhimian;

DELETE FROM question_tag WHERE question_id BETWEEN 101 AND 199;
DELETE FROM question WHERE id BETWEEN 101 AND 199;

INSERT INTO question (id, title, content, answer, difficulty, category_id, view_count, create_user_id) VALUES
  (101, '过滤器和拦截器的区别', NULL, '过滤器和拦截器的核心区别有几点:归属上,过滤器是 Servlet 规范定义的,依赖 Servlet 容器;拦截器是 Spring MVC 提供的,依赖 Spring 容器。实现上,Filter 实现 Filter 接口的 doFilter 方法,基于函数回调;Interceptor 实现 HandlerInterceptor 接口,有 preHandle、postHandle、afterCompletion 三个方法。执行时机上,Filter 在 DispatcherServlet 之前执行,能拦截所有请求包括静态资源;Interceptor 在 DispatcherServlet 之后、Controller 前后执行,只拦截进入 Spring MVC 流程的请求,整体顺序是 Filter前 → preHandle → Controller → postHandle → afterCompletion → Filter后。能力上,拦截器能注入 Spring 容器的 Bean、能拿到 HandlerMethod 知道请求打到哪个方法,过滤器只能操作 Request 和 Response。场景上,Filter 适合统一编码、XSS 防护、跨域这类通用处理;Interceptor 适合登录鉴权、权限校验这类需要结合业务和 Spring 上下文的场景。', 2, 11, 0, 1),
  (102, '抽象类和接口的区别', NULL, '这两者语法上的区别主要有:抽象类可以有构造方法、任意成员变量和具体方法实现,而接口没有构造方法,变量只能是常量,Java 8 之前方法必须是抽象的,之后才支持 default 和 static 方法;另外类只能单继承抽象类,但可以多实现接口。

设计层面上,抽象类表达的是 is-a 关系,是对一类事物共性的抽象,适合做模板复用;接口表达的是 can-do 关系,是对行为能力的抽象,适合定义规范、实现解耦。

实际选择时,如果需要代码复用和状态就用抽象类,只定义能力规范就用接口,很多框架比如 MyBatis 的 Executor 就是接口定规范、抽象类做骨架,两者配合使用。', 1, 13, 0, 1),
  (103, 'String、StringBuffer、StringBuilder 的区别', NULL, '这三者的区别主要在可变性、线程安全和性能三个方面。

String 是不可变的,底层字符数组用 final 修饰,任何修改都会生成新对象,所以频繁拼接时性能最差;StringBuffer 和 StringBuilder 都是可变的,继承自同一个父类 AbstractStringBuilder,在原对象上修改。

线程安全方面,String 因为不可变天然线程安全;StringBuffer 的方法加了 synchronized,是线程安全的;StringBuilder 没加锁,线程不安全,但也因此性能最好。

使用场景上:字符串不怎么变化就用 String;单线程下频繁拼接用 StringBuilder,这也是最常用的;多线程共享同一个字符串缓冲区才用 StringBuffer,实际开发中比较少见,因为一般用局部变量的 StringBuilder 就没有线程安全问题。

另外补充一点,String 设计成不可变是为了支持字符串常量池、缓存 hashCode 以及保证安全性,这也是它适合做 HashMap key 的原因。', 1, 13, 0, 1),
  (104, 'Java 中的基本数据类型和引用数据类型及大小', NULL, 'Java 有 8 种基本数据类型,分四类:整型有 byte、short、int、long,分别占 1、2、4、8 个字节;浮点型有 float 和 double,占 4 和 8 个字节;字符型 char 占 2 个字节,用 Unicode 编码,所以能存中文;布尔型 boolean 比较特殊,JVM 规范没有明确规定大小,单个变量按 int 处理占 4 字节,boolean 数组里每个元素按 1 字节处理。

除了这 8 种,其他都是引用数据类型,包括类、接口、数组、枚举和注解。引用变量本身在 64 位 JVM 下占 8 字节,不过开启指针压缩后是 4 字节。

补充一点,基本类型局部变量直接把值存在栈上,引用类型栈上存地址、对象在堆里。另外每个基本类型都有对应的包装类,像 Integer 有 -128 到 127 的缓存池,这个范围内用 == 比较是相等的,超出就不等了,这是实际开发中容易踩的坑。', 1, 13, 0, 1),
  (105, 'Comparable 和 Comparator 有什么区别', NULL, '这两个都是用来做对象排序的接口,核心区别在于比较逻辑放在哪里。

Comparable 是内部比较器,在 java.lang 包下,类自己实现它的 compareTo 方法,把比较规则写在类内部,代表这个类的自然排序,比如 String、Integer 都实现了它。缺点是一个类只能有一种排序规则,而且要改类的源码。

Comparator 是外部比较器,在 java.util 包下,比较规则定义在类外面,不侵入原类。它的优势是灵活:可以为同一个类定义多种排序规则,也能给改不了源码的第三方类排序。Java 8 之后它还是函数式接口,配合 lambda 和 comparing、thenComparing 这些方法用起来很方便。

实际使用时,像 Collections.sort 或者 TreeMap 这些,如果传了 Comparator 就优先用它,没传就要求元素实现 Comparable,都没有就会抛类型转换异常。

简单总结就是:Comparable 是"我自己会比较",一种固定规则;Comparator 是"第三方帮你比较",是策略模式的应用,想怎么比就传什么策略,更灵活。', 1, 13, 0, 1),
  (106, 'int 和 Integer 的区别', NULL, 'int 是基本数据类型,占 4 字节,直接存数值;Integer 是它的包装类,是引用类型,对象存在堆里。

主要区别有几点:一是默认值,int 是 0,Integer 是 null,所以像数据库里可能为空的字段,实体类要用 Integer 才能表达"没有值"的状态;二是 Integer 能用在泛型里,比如 List 集合只能存包装类;三是 Integer 提供了 parseInt 这些工具方法。

两者之间有自动装箱和拆箱机制,装箱调 valueOf,拆箱调 intValue。这里有个高频考点:Integer 有缓存池,valueOf 对 -128 到 127 范围内的值会复用缓存对象,所以两个 127 用 == 比较是 true,两个 128 就是 false 了。因此 Integer 比较相等一定要用 equals,不能用 ==。

实际开发还要注意一个坑:Integer 为 null 时自动拆箱会抛空指针异常,比如把 null 的 Integer 赋给 int,或者在三目运算符里混用 int 和 Integer,这些都是线上容易出问题的地方。', 1, 13, 0, 1),
  (107, 'Java 中的异常处理机制', NULL, 'Java 的异常体系顶层是 Throwable,下面分两支:Error 和 Exception。Error 是 JVM 层面的严重错误,比如内存溢出、栈溢出,程序处理不了;Exception 才是我们要处理的,又分为受检异常和运行时异常——受检异常像 IOException,编译器强制要求捕获或者 throws 声明;运行时异常像空指针、数组越界,编译器不强制处理,一般是代码逻辑问题,应该靠修复代码来避免。

处理机制上有五个关键字:try 包住可能出异常的代码,catch 捕获处理,finally 做资源释放,throw 主动抛出异常对象,throws 在方法上声明异常交给调用方。这里有几个细节:多个 catch 要子类在前父类在后;finally 除了 System.exit 这种情况外一定会执行,但不能在 finally 里写 return,会覆盖原返回值还会吞异常;Java 7 之后推荐用 try-with-resources 自动关闭资源,代码更简洁也不会出现异常被屏蔽的问题。

底层实现上,编译器会在字节码里生成异常表,记录代码范围、异常类型和跳转位置,JVM 抛异常时沿着调用栈回溯找处理器。因为构建异常要抓取整个调用栈,开销比较大,所以不能拿异常来控制业务流程。

在我的毕设项目里,我的做法是自定义业务异常继承 RuntimeException,然后用 Spring 的 @RestControllerAdvice 做全局异常处理,统一拦截转换成错误码和提示信息返回给前端,这样 Controller 层就不用到处写 try-catch,代码也更干净。', 2, 13, 0, 1),
  (108, '反射的用途及实现原理', NULL, '反射是 Java 在运行时动态获取类的结构信息、并操作类和对象的能力,比如运行时创建对象、调用方法、修改属性,哪怕是私有的也可以。核心类是 Class、Field、Method、Constructor,入口是获取 Class 对象,常用 Class.forName、类名.class 或者对象的 getClass 这几种方式。

它的用途主要体现在框架上:Spring 的 IOC 容器就是通过反射创建 Bean、给 @Autowired 的字段注入值;JDK 动态代理底层靠反射 invoke 目标方法,这是 Spring AOP 和 MyBatis Mapper 接口的基础;还有 MyBatis 的结果集映射、Jackson 的 JSON 序列化、JDBC 加载驱动,本质都在用反射。可以说反射是各种框架的基石。

实现原理上,类加载的时候 JVM 会把字节码的元数据存到方法区,同时在堆里生成唯一的 Class 对象作为访问入口,反射拿到的信息其实就是来自这份元数据。再深入一点,Method.invoke 有个"膨胀机制":前期通过 JNI 调 native 方法执行,当同一个方法反射调用超过 15 次,JVM 会动态生成字节码,把反射调用变成直接调用,大幅提升性能,这是启动速度和运行速度之间的权衡。另外 setAccessible(true) 的原理就是设置标志位跳过访问权限检查,所以既能访问私有成员也能提速。

反射的缺点是性能比直接调用慢、破坏封装性,还会绕过编译期的类型检查,所以业务代码里一般不直接用,主要是框架和中间件在用,使用时通常会把 Method 对象缓存起来减少开销。', 2, 13, 0, 1),
  (109, '如何实现线程的同步', NULL, '线程同步要解决的是多线程并发访问共享资源时的数据不一致问题,本质是保证原子性、可见性和有序性。实现方式我按几类来说:

第一类是加锁。最基础的是 synchronized 关键字,可以修饰实例方法、静态方法或者代码块,分别锁 this、Class 对象和指定对象,它基于对象头和 Monitor 实现,JDK 6 之后有偏向锁、轻量级锁到重量级锁的锁升级优化,而且能自动释放锁。更灵活的是 JUC 的 ReentrantLock,支持 tryLock 超时获取、可中断、公平锁,还能绑定多个 Condition 做精确唤醒,底层基于 AQS 实现,不过要注意必须在 finally 里手动 unlock;读多写少的场景还可以用读写锁。

第二类是无锁方案。volatile 保证可见性和有序性,但不保证原子性,适合做状态标志位;原子类像 AtomicInteger 基于 CAS 实现,适合计数场景,高并发下用 LongAdder 更好,CAS 要注意 ABA 问题。

第三类是线程协作工具,比如 CountDownLatch 让线程等待其他线程完成,CyclicBarrier 让一组线程到齐再一起执行,Semaphore 控制并发数做限流,再加上 wait/notify 或 Condition 的等待通知机制,可以实现生产者消费者这类协作。

另外还有一个思路是避免共享:用 ThreadLocal 给每个线程独立副本,或者直接用 ConcurrentHashMap、阻塞队列这些并发容器,以及不可变对象。

实际选择上,简单互斥我优先用 synchronized,因为 JVM 一直在优化它,代码也不容易出错;需要超时、中断这些高级特性才上 ReentrantLock;单纯计数就用原子类;能不共享就不共享,这是并发设计里更优先的思路。', 2, 3, 0, 1),
  (110, '什么是守护线程,与普通线程的区别', NULL, 'Java 里线程分两类:用户线程和守护线程。守护线程是为其他线程提供后台服务的线程,最典型的就是 GC 垃圾回收线程。

它和普通线程最核心的区别就一点:JVM 的退出只取决于用户线程——只要还有一个用户线程在运行,JVM 就不会退出;而当所有用户线程都结束后,不管守护线程任务有没有执行完,JVM 都会直接退出,守护线程被强制终止。所以守护线程可以理解为"陪跑"的角色,主角都下班了,它也就没有存在的意义了。

使用上通过 setDaemon(true) 设置,有几个细节要注意:必须在 start 之前设置,否则抛异常;守护性会继承,守护线程里创建的线程默认也是守护线程;我们平时创建的线程默认都是用户线程,因为 main 线程是用户线程。

工程实践上最重要的一点是:守护线程不能执行重要的业务逻辑,比如写文件、操作数据库,因为它随时可能被 JVM 强制掐断,连 finally 块都不保证执行,数据可能写一半就丢了。它适合做心跳检测、缓存清理、监控上报这种和主程序同生共死的辅助任务。另外还有个常见问题:程序 main 方法跑完了却不退出,往往就是线程池里的用户线程还活着,这时候要么显式 shutdown,要么用自定义 ThreadFactory 把线程池的线程设成守护线程。', 1, 3, 0, 1),
  (111, 'Java 集合框架的核心接口', NULL, 'Java 集合框架有两大顶级接口:Collection 和 Map,两者是并列的,Map 不继承 Collection。

Collection 是单列集合的根接口,它上面还有个 Iterable 提供迭代能力。Collection 下面有三大子接口:第一个是 List,特点是有序、可重复、支持索引访问,常用实现有底层是动态数组的 ArrayList,查询快增删慢;还有双向链表的 LinkedList,头尾操作快。第二个是 Set,特点是元素不可重复,去重靠 hashCode 加 equals,常用的有基于 HashMap 实现的 HashSet、保持插入顺序的 LinkedHashSet 和基于红黑树自动排序的 TreeSet。第三个是 Queue 队列,先进先出,它的子接口 Deque 是双端队列,ArrayDeque 既能当队列也能当栈用,官方推荐它替代老的 Stack;并发包里的 BlockingQueue 阻塞队列则是线程池的基础。

Map 是双列集合的根接口,存键值对,key 不能重复。最常用的是 HashMap,JDK 8 之后底层是数组加链表加红黑树;需要保持顺序用 LinkedHashMap,它还能实现 LRU 缓存;需要按 key 排序用红黑树实现的 TreeMap;并发场景用 ConcurrentHashMap,它在 JDK 8 里用 CAS 加 synchronized 锁单个桶,替代了粗粒度锁的 Hashtable。

另外补充一点,普通集合的迭代器有 fail-fast 机制,遍历中结构被修改会抛 ConcurrentModificationException,而并发容器是 fail-safe 的,这也是选型时要考虑的点。', 1, 13, 0, 1),
  (112, '对 Java 序列化的理解', NULL, '序列化就是把 Java 对象转成字节序列的过程,反序列化则是反过来。因为对象是活在 JVM 内存里的,想把它存到磁盘、放进 Redis 或者在网络上传输,比如 RPC 调用,就必须先变成字节流,这就是序列化的意义。

Java 原生的做法是实现 Serializable 接口,它是个标记接口,没有方法,然后通过 ObjectOutputStream 和 ObjectInputStream 读写对象。这里有几个关键点:

一是 serialVersionUID,它是类的版本号,反序列化时会比对,不一致就抛异常。如果不显式声明,编译器会根据类结构自动生成,那么类只要一改动,旧数据就全部反序列化失败,所以规范要求必须显式声明。

二是 transient 关键字,修饰的字段不参与序列化,适合密码这类敏感信息,反序列化后是默认值;static 字段也天然不序列化,因为它属于类不属于对象。

三是一些细节:成员对象也必须可序列化,否则抛 NotSerializableException;反序列化不走构造方法,所以会破坏单例,需要通过 readResolve 方法防御,或者直接用枚举单例;像 ArrayList 还通过自定义 writeObject 只序列化实际元素而不是整个数组容量,来优化体积。

不过 Java 原生序列化在实际项目中用得少,因为它只支持 Java、性能差、字节流体积大,还有反序列化漏洞的安全风险。所以实践中 HTTP 接口和 Redis 缓存一般用 JSON,比如 Jackson;RPC 框架用 Hessian、Protobuf、Kryo 这些高性能的二进制方案,像 Dubbo 默认就是 Hessian2。', 2, 13, 0, 1),
  (113, '对泛型的理解', NULL, '泛型是 JDK 5 引入的参数化类型机制,就是把类型当作参数传给类、接口或方法。它的价值主要有三点:一是编译期类型安全,比如往 List<String> 里放 Integer 直接编译报错,把问题从运行时提前到编译期;二是省去了取元素时的强制类型转换;三是提高代码复用,像整个集合框架、通用的 Result<T> 响应封装都是靠泛型一套代码适配所有类型。

泛型有泛型类、泛型接口和泛型方法三种形式,泛型方法的类型参数声明在返回值前面,可以独立于类存在。

它最核心的原理是类型擦除:Java 泛型只存在于编译期,编译后类型参数会被擦除,无界的擦成 Object,有上界的擦成上界类型,同时编译器会自动插入强制转换和生成桥方法来保证正确性。所以运行时 List<String> 和 List<Integer> 其实是同一个 Class。擦除也带来一些限制,比如不能 new T()、不能对泛型用 instanceof、静态成员不能用类的类型参数、两个方法参数分别是 List<String> 和 List<Integer> 会因为擦除后签名相同而无法重载。

另外一个重点是通配符:? extends T 是上界通配符,只能读不能写;? super T 是下界通配符,可以写但读出来只能当 Object。选择上遵循 PECS 原则——生产者用 extends,消费者用 super,JDK 里 Collections.copy 的方法签名就是典型例子。还要注意泛型不支持协变,List<Integer> 不是 List<Number> 的子类型,这一点和数组不同,也正因为这样泛型比数组协变更安全。

实际开发中我在毕设里就用泛型封装了统一响应类 Result<T>,配合 MyBatis-Plus 的 BaseMapper<T>,一套代码就能适配所有实体的增删改查。', 2, 13, 0, 1),
  (114, '泛型实战:统一响应类 Result<T> 的设计', NULL, '我在毕设里封装了统一响应类 Result<T>,它包含 code、message 和一个泛型的 data 字段。这个类把泛型的几个知识点都用上了:首先它是泛型类,data 类型由每个接口自己决定,返回帖子详情就是 Result<PostVO>,分页就是 Result<Page<PostVO>>,泛型还能嵌套;其次它提供了 success、error 这些静态工厂方法,这里有个细节——静态方法不能用类上的 T,因为静态成员不依附实例,所以要在方法返回值前面单独声明 <T>,这是一个独立的类型参数;调用的时候编译器会自动做类型推断,不用显式写类型。

这样做的好处是编译期就保证了类型安全,getData 拿到的直接就是具体类型,不用强转,类型写错了编译就不通过。另外我也遇到过类型擦除的实际影响:泛型信息运行时是没有的,所以用 HTTP 客户端反序列化 Result<PostVO> 时 data 会变成 LinkedHashMap,需要用 TypeReference 类型令牌来保留泛型信息,这算是擦除机制在实际开发里最常见的一个坑。', 2, 13, 0, 1),
  (115, '字符串字面量与 new String 的区别', NULL, '这两种方式的核心区别在于对象的创建位置和数量。

用字面量 String s = "hello" 时,JVM 会先查字符串常量池,有就直接复用池里的对象,没有才创建一个放进去,所以最多创建一个对象,相同字面量全局共享。而 new String("hello") 不管常量池有没有,都会在堆里新建一个对象:如果常量池已经有 "hello" 就只创建堆里这一个;如果没有,字面量还要先进常量池,总共创建两个对象。

所以两个字面量 "hello" 用 == 比较是 true,因为指向池中同一个对象;字面量和 new 出来的比较是 false,两个 new 出来的互相比也是 false,要比内容必须用 equals。

再深入一点,常量池 JDK 7 之后从永久代移到了堆里,底层是一个叫 StringTable 的哈希表。String 提供了 intern 方法可以主动把字符串放进池里,JDK 7 之后如果池里没有,intern 不会复制字符串,而是直接把堆对象的引用记录进池,这也是那道经典的 new String 拼接后 intern 再比较的面试题在 JDK 6 和 7 结果不同的原因。

还有一个容易混淆的点是编译期优化:两个字面量常量相加,比如 "he" + "llo",编译期会直接折叠成 "hello" 进常量池;但只要拼接里含有变量,就是运行期通过 StringBuilder 拼出来的,结果在堆中,和常量池对象 == 比较就是 false。

实践上永远推荐用字面量方式创建,new String 只会白白多创建对象,没有任何必要;字符串内容比较一律用 equals。', 2, 13, 0, 1),
  (116, '创建一个对象或变量的流程', NULL, '首先要区分变量和对象:变量如果是局部变量,存在虚拟机栈的栈帧的局部变量表里,基本类型直接存值,引用类型存的是对象地址;对象本体永远在堆中;如果是成员变量,那它跟随对象一起存在堆里,包括基本类型的成员变量。

new 一个对象在 JVM 层面分五步:第一步类加载检查,确认类已经加载初始化过,没有就先触发类加载;第二步分配内存,堆规整就用指针碰撞,不规整就用空闲列表,并发安全靠 CAS 重试或者 TLAB 线程私有缓冲区解决;第三步把内存初始化为零值,这也是成员变量有默认值而局部变量必须手动初始化的原因;第四步设置对象头,包括 Mark Word 和指向类元数据的类型指针;第五步执行 init 构造方法,先父类后子类,按显式赋值、实例代码块、构造器的顺序执行,对象才真正可用,最后把地址赋给栈上的引用变量。

拿 new String("hello") 来说就是:字面量 hello 在类加载解析时进入字符串常量池;运行到这行时在堆里新建一个 String 对象,它内部的字符数组和常量池对象共享;最后栈上的变量 s 存这个堆对象的地址。所以是三个位置:引用在栈、new 的对象在堆、字面量在常量池,常量池本身 JDK 7 之后也位于堆中。', 2, 4, 0, 1),
  (117, '自定义注解的场景及实现', NULL, '注解本质是一种元数据标记,它自己不产生任何行为,必须配合处理程序才有意义,所以自定义注解就是"定义注解 + 写处理逻辑"两件事。

定义方面,用 @interface 声明,再用四个元注解描述它:@Target 限定能标在哪,比如类上还是方法上;@Retention 指定生命周期,自定义注解基本都用 RUNTIME,这样运行时才能通过反射读到,默认的 CLASS 级别反射是读不到的,这是个常见的坑;另外还有 @Documented 和 @Inherited。注解里可以定义属性,支持基本类型、String、Class、枚举和数组,可以给默认值。

处理方面主要三种方式:最常用的是运行时反射,配合拦截器或 AOP;其次是编译期的 APT 注解处理器,像 Lombok 就是编译时生成代码,零运行时开销;还有字节码增强。

实际场景我拿毕设举例:我的论坛系统有普通用户、版主、管理员三种角色,我自定义了一个 @RequireRole 注解标在 Controller 方法上,声明这个接口允许哪些角色访问,然后在登录拦截器里通过 HandlerMethod 反射读取这个注解,再和 JWT 里解析出来的角色比对,不匹配就抛 403。这样权限控制从每个方法里重复的 if 判断变成了一行声明式注解,新增接口只需要打个标记。另外像操作日志也很典型,定义 @Log 注解,用 AOP 的 @annotation 切点拦截,自动记录操作描述、参数和耗时,业务代码零侵入。其他常见场景还有接口幂等、限流、参数校验、字段脱敏,思路都是一样的——注解做声明,AOP 或拦截器做处理。

其实 Spring 的 @Transactional、@Autowired 就是这个模式最大的应用。使用时要注意一点:基于 Spring AOP 处理的注解,同类内部方法自调用会不走代理导致注解失效,这也是 @Transactional 失效的经典原因。', 2, 13, 0, 1),
  (118, 'Spring Boot 引入 starter 依赖就能直接用的原理(自动配置)', NULL, '这背后是 Spring Boot 的两个核心机制:起步依赖和自动配置。

Starter 本身没什么代码,它是个依赖描述符,通过 Maven 传递依赖把一个功能需要的全套 jar 和兼容的版本一次性引进来,版本由 spring-boot-dependencies 统一管理,解决的是"要引哪些包"的问题。

而"引进来就能用"靠的是自动配置。入口是 @SpringBootApplication 里的 @EnableAutoConfiguration,它通过 @Import 导入 AutoConfigurationImportSelector,启动时去读取各个 jar 包里 META-INF 下的注册文件——2.7 之前是 spring.factories,新版本是 AutoConfiguration.imports——拿到一百多个候选的自动配置类,这是一种 SPI 思想。

但这些配置类不会全部生效,关键在条件装配。每个自动配置类上都有 @Conditional 系列注解:@ConditionalOnClass 判断类路径下有没有相关的类,所以只有引了对应依赖,这个配置类才激活;激活后它通过 @Bean 把默认组件注册进容器,比如引了 Redis 依赖就自动注册 RedisTemplate;同时 @ConditionalOnMissingBean 保证如果我自己定义了同类型的 Bean,默认的就不生效,也就是用户配置优先;配置参数则通过 @ConfigurationProperties 从 yml 里绑定,没配就走默认值,这就是约定优于配置。

串起来就是:启动时读注册文件拿候选配置类,条件注解层层过滤,幸存的向容器注册默认 Bean,属性从配置文件绑定,用户自定义优先。

我自己也写过简单的自定义 starter 验证过这套流程:写好自动配置类和 Properties 类,在 imports 文件里登记,别的项目一引依赖功能就直接可用。排查的时候可以加 --debug 启动,看条件评估报告,哪些配置生效、哪些没生效一目了然;不需要的自动配置也可以用 exclude 排除,比如经典的排除 DataSourceAutoConfiguration。', 2, 11, 0, 1),
  (119, 'Object 类的常用方法', NULL, 'Object 是所有类的超类,它的方法我按四类来说:

第一类是对象比较的 equals 和 hashCode。equals 默认比较内存地址,等价于 ==,要内容相等的语义就得重写,并遵守自反性、对称性、传递性这些约定。hashCode 返回哈希值,它和 equals 有个强约定:equals 相等则 hashCode 必须相等,反过来不成立。所以重写 equals 必须同时重写 hashCode,否则在 HashMap、HashSet 里会出问题——两个相等的对象落到不同的桶,去重失效、存进去取不出来。

第二类是对象表示的 toString,默认输出类名加哈希值,没有可读性,实体类一般都重写或者用 Lombok 生成,方便打日志排查问题。

第三类是对象操作相关:getClass 返回运行时的实际类型,是反射的入口,多态下拿到的是子类的 Class;clone 用于对象拷贝,前提是实现 Cloneable 接口,默认是浅拷贝,引用字段只复制地址,需要深拷贝就得逐层克隆或者用序列化的方式;还有个 finalize,是 GC 回收前的回调,因为执行时机不确定、性能差,JDK 9 已经废弃了,资源清理应该用 try-with-resources。

第四类是线程协作的 wait、notify 和 notifyAll,配合 synchronized 实现等待通知机制,wait 会释放锁进入对象的等待队列,notify 随机唤醒一个,notifyAll 唤醒全部,而且必须在同步块里调用,否则抛非法监视器状态异常。这里有个经典追问是为什么这组方法定义在 Object 而不是 Thread——因为 Java 里锁是对象级别的,任何对象都能当监视器,等待队列挂在对象上,线程等的是"某个对象的锁",所以自然属于 Object。顺带一提 wait 和 sleep 的区别:wait 释放锁、要在同步块里、靠 notify 唤醒;sleep 是 Thread 的静态方法、不释放锁、到点自己醒。', 1, 13, 0, 1),
  (120, '哪些情况会导致索引失效', NULL, '索引失效的场景我按原因归几类说,核心原理是 B+ 树靠原始值的有序性定位,破坏了这个前提索引就用不上了。

第一类是对索引列做了加工:比如在索引列上套函数、做运算,像 DATE(create_time) 等于某天,索引里存的是原始值,加工后的值没有有序结构,只能全表扫,改法是把运算移到常量那一侧,换成范围查询;更隐蔽的是隐式类型转换,比如 varchar 的手机号字段用数字去查,MySQL 的规则是把字符串转成数字,相当于给索引列套了 CAST 函数,直接失效,这个坑我觉得最阴险,因为 SQL 看起来完全正常;还有 join 时两表关联字段字符集不一致,也会触发隐式转换导致被驱动表索引失效。

第二类是联合索引违反最左前缀:索引 (a,b,c) 是先按 a 排、再按 b 排、再按 c 排的,查询不带 a 就完全失效;带 a 跳过 b 直接查 c,c 用不上;a 等值、b 范围查询,那 b 右边的 c 也失效,因为范围内下一列不再有序。所以建联合索引要把等值列放前面、范围列放最后。另外条件的书写顺序无所谓,优化器会自己调整。

第三类是 like 以百分号开头,前缀不确定没法在 B+ 树里定位;后缀匹配的需求可以冗余反转列,或者交给 ES。

第四类是 OR 连接了没有索引的列,因为另一边横竖要全表扫,整条 SQL 就都不走索引了,解决办法是补索引或者拆成 UNION ALL。

第五类其实不是失效,而是优化器算完账主动放弃:比如查性别这种区分度极低的列,或者用 != 、NOT IN 筛出大半个表的数据,二级索引要回表、是随机 IO,当返回行数超过全表百分之二三十,优化器认为全表顺序扫描反而更便宜。还有统计信息过期导致误判,可以 ANALYZE TABLE 更新,必要时 FORCE INDEX。

排查的时候就是看 EXPLAIN:type 是不是 ALL、key 有没有用上预期索引、rows 扫描量、Extra 里有没有 filesort 这些信号,再深入可以开 OPTIMIZER_TRACE 看优化器的决策过程。', 2, 5, 0, 1),
  (121, '并发事务的数据一致性问题与隔离级别', NULL, '并发事务我主要关注四个问题:脏读,读到别人没提交的数据,对方一回滚我就拿着假数据在跑业务;不可重复读,同一个事务里两次读同一行值不一样,因为中间被别人改了并提交,侧重点是值变了;幻读,同一个条件两次范围查询行数不一样,因为别人插入了新行,侧重点是行数变了,针对的是 insert;另外还有丢失更新,两个事务基于同一个旧值计算后互相覆盖,这个隔离级别管不全,要靠原子更新或者乐观锁解决。

四种隔离级别是逐级解决这些问题的:读未提交什么都不解决,基本没人用;读已提交解决脏读,Oracle 和 PG 的默认级别;可重复读再解决不可重复读,是 MySQL 的默认级别;串行化全部解决但性能太差。

结合 InnoDB 的实现说,RC 和 RR 的区别本质在 MVCC 的 ReadView 生成时机:RC 每条查询都生成新快照,所以总能看到别人最新提交的,才会不可重复读;RR 只在事务第一次查询时生成快照并全程复用,所以两次读结果一致。至于幻读,RR 下要分两种读:普通 select 是快照读,靠 MVCC 天然看不到新插入的行;update、for update 这些是当前读,靠间隙锁也就是 Next-Key Lock 把范围锁住不让插入。所以说 RR 基本解决了幻读,但不彻底——如果事务里先快照读、别人插入提交后我又 update 碰到了那行,再查就能看见了,要彻底防就得显式加锁。

实际项目里我在毕设的会议室预约系统碰到过这类问题:两个用户同时约同一时段,RR 的快照读互相看不见对方的插入,都会预约成功。我的处理是三层:会议室加时段建唯一索引做数据库层兜底,核心校验用 select for update 当前读加锁,前面再用 Redis 分布式锁挡并发。这个经历让我理解隔离级别只是通用保障,关键业务的一致性还是要靠唯一约束和乐观、悲观锁主动控制。另外补充一点,很多互联网公司会把 MySQL 调成 RC,因为间隙锁少、死锁概率低、并发度更高,业务上能容忍不可重复读。', 2, 5, 0, 1),
  (122, '对设计模式的理解', NULL, '设计模式是前人对软件开发中反复出现的问题总结出的可复用解决方案,GoF 总结了 23 种,分创建型、结构型、行为型三大类。我的理解是:模式是招式,背后的设计原则才是内功,核心就是 SOLID,尤其是开闭原则——对扩展开放、对修改关闭,以及依赖倒置——面向抽象编程,所有模式本质上都是在"封装变化"。

三类模式各说几个最常用的:创建型里单例保证全局唯一实例,手写双重检查锁要注意 volatile 防指令重排,否则可能拿到半初始化对象,更优雅的是静态内部类和枚举;工厂模式把创建和使用解耦,Spring 的 BeanFactory 就是;建造者解决构造参数过多,像 Lombok 的 @Builder。结构型里最重要的是代理,JDK 动态代理基于接口、CGLIB 基于继承,这是 Spring AOP 和 MyBatis Mapper 接口的底层;还有装饰器,Java IO 流一层包一层就是教科书案例;适配器做接口转换,SpringMVC 的 HandlerAdapter。行为型里策略模式用可互换的算法消除 if-else,Comparator 就是策略;模板方法父类定骨架子类填细节,JdbcTemplate、AQS 都是;观察者做事件解耦,Spring 的事件机制;责任链像 Filter 链、拦截器链。

实际项目里我用过几个:毕设论坛系统的内容审核,帖子、评论、私信规则不同,我用策略模式定义 AuditStrategy 接口,Spring 注入 Map 按类型取策略,替代了一堆 if-else,新增类型只需要加一个实现类;发帖后的积分、推送这些附加逻辑我用 Spring 事件观察者模式解耦,主流程不受影响;通知功能用模板方法抽了固定流程。

最后我想强调一点态度:设计模式是手段不是目的,过度设计本身就是反模式。我的做法是先写简单的代码,等变化真正出现、分支开始膨胀了再重构引入模式,让模式为需求服务,而不是反过来。', 2, 13, 0, 1),
  (123, '什么是值传递和引用传递', NULL, '值传递是把实参的值复制一份传给形参,方法内改的是副本,不影响原变量;引用传递是把实参本身传进去,形参就是实参的别名,方法内的修改直接作用于原变量。

关于 Java,先给结论:Java 只有值传递,没有引用传递。很多人说"基本类型值传递、引用类型引用传递",这是不对的。基本类型复制的是值本身;引用类型复制的是引用的副本,也就是地址的拷贝——传的这个"值"恰好是个地址,但依然是拷贝,所以本质还是值传递。

验证方法很简单,看一个试金石场景:方法里给形参重新赋值,比如 u = new User(),如果外面的变量也跟着指向新对象,那才是引用传递;实际在 Java 里外面纹丝不动,所以是值传递。而大家产生"引用传递"错觉的场景是:方法里通过形参调 setter 修改对象内容,外面能看到变化——那是因为副本引用和原引用指向堆里同一个对象,改的是对象内部状态,不是改引用本身,这恰恰是"传递了地址的值"的表现。

两个经典佐证:一是 String 传进方法怎么改外面都不变,因为传的是引用副本,加上 String 不可变,重新赋值只是让副本指向新字符串;二是 Java 写不出真正的 swap 交换方法,C++ 用引用参数可以,Java 语法层面就没有引用传递这个能力。

实践上要注意的是:方法内修改传入对象的内容是有副作用的,会影响调用方,所以设计接口时要么文档说明,要么用不可变对象或防御性拷贝来避免数据被意外篡改。', 1, 13, 0, 1),
  (124, '构造器是否可被重写', NULL, '构造器不能被重写,但可以被重载。

不能重写的原因有三层:第一,重写的前提是继承,而构造器不被子类继承;第二,构造器的名字必须和类名一致,父类构造器和父类同名,子类构造器和子类同名,子类里根本写不出一个跟父类构造器同名的构造器,连重写要求的"方法签名相同"都不可能满足;第三,从原理上讲,重写的意义是运行时多态,靠虚方法表动态绑定,而构造器在字节码里是 init 方法,通过 invokespecial 指令静态绑定,编译期就确定了,天生不具备多态性,语义上构造器只负责初始化本类的状态,也不该被子类改写。

但构造器支持重载,同一个类里定义多个参数列表不同的构造器,还可以用 this() 互相调用来复用初始化逻辑,这在开发里非常常见。

另外子类和父类构造器之间的真实关系是链式调用而不是重写:子类构造器第一行必须先调 super() 完成父类初始化,不写编译器会默认加一个无参的 super(),所以如果父类只定义了带参构造而没有无参构造,子类就必须显式调用 super 传参,否则编译不过,这是个常踩的坑。整体的初始化顺序是先父类后子类:静态代码块、实例代码块、构造器依次执行。

顺带一提,构造器不能用 final、static、abstract 修饰,但可以是 private 的,比如单例模式就是私有化构造器来禁止外部创建对象。', 1, 13, 0, 1),
  (125, 'for-each 与常规 for 循环的效率区别', NULL, '要比较效率得先知道 for-each 的本质:它是语法糖,编译时对数组会翻译成普通的下标 for 循环,对集合会翻译成 Iterator 迭代器遍历。所以真正比的是"下标访问"和"迭代器访问"的差异。

分场景说:遍历数组,两者编译后基本一样,没有效率差别。遍历 ArrayList,普通 for 的 get(i) 是数组下标直接访问,for-each 走迭代器,每次 next 多一点方法调用和 modCount 检测的开销,普通 for 理论上略快,但差距是纳秒级的,JIT 优化后实际可以忽略。

差距真正大的是 LinkedList:它是链表,没有下标,普通 for 每次 get(i) 都要从头遍历到第 i 个节点,单次 O(n),整个循环就是 O(n²),十万条数据就能明显卡住;而 for-each 的迭代器内部记着当前节点,每次 next 只是指针后移,整体 O(n)。所以链表用普通 for 加 get(i) 是典型的性能事故。JDK 为此专门设计了 RandomAccess 标记接口,ArrayList 实现了它表示支持快速随机访问,适合下标遍历;LinkedList 没实现,就必须用迭代器,JDK 源码比如 Collections.binarySearch 里就有这个 instanceof 判断来选择遍历方式。

效率之外还有几个使用差异:for-each 拿不到下标,不能控制方向和步长;遍历中不能直接增删元素,会触发 fail-fast 抛 ConcurrentModificationException,要删除必须用 Iterator 的 remove 方法;另外像 Set、Map 这种没有下标的集合只能用 for-each 或迭代器。

实践上我的原则是:需要下标或者要边遍历边按位置操作时用普通 for;其他情况优先 for-each,更简洁也不会写错边界;LinkedList 这类链式结构坚决不用 get(i) 遍历。', 1, 13, 0, 1),
  (126, '两个不相等的对象有可能具有相同 hashCode 吗', NULL, '完全可能,这就是哈希冲突,而且是必然存在的。

从规范讲,hashCode 和 equals 的约定是单向的:equals 相等则 hashCode 必须相等;但反过来,hashCode 相等不要求 equals 相等,所以两个不相等的对象拥有相同 hashCode 不违反任何契约。

从数学上讲这也不可避免:hashCode 返回 int,只有 42 亿多种取值,而对象是无限的,按鸽巢原理,无限的对象映射到有限的哈希值上必然产生碰撞。有个经典例子,字符串"Aa"和"BB"的 hashCode 都是 2112,中文的"通话"和"重地"哈希值也相同,因为 String 的哈希算法是 31 倍乘法累加,不同的字符组合完全可能算出同一个结果。

正因为冲突不可避免,HashMap 这类哈希表都有冲突解决机制。JDK 8 的 HashMap 用链地址法:先用扰动后的 hash 定位桶,同一个桶里的元素再用 equals 逐个比较,equals 相等就覆盖,不等就挂链表,链表超过 8 且数组容量到 64 就树化成红黑树,把查询从 O(n) 降到 O(log n)。

这也反过来解释了为什么重写 equals 必须重写 hashCode:HashMap 是先 hashCode 定桶、再 equals 确认的两步机制,如果相等的对象哈希值不同,会落进不同的桶,导致重复存储、get 找不到,破坏正确性;而不相等的对象哈希相同只是多比几次 equals,只影响性能不影响正确性。

最后补充一点工程视角:让 hashCode 恒返回 1 是完全合法的实现,但所有元素挤一个桶,性能退化,所以好的 hashCode 要让分布尽量均匀,标准做法是用 Objects.hash 这种 31 倍哈希;另外恶意构造同哈希的 key 还能发起哈希碰撞攻击,JDK 8 引入红黑树也有防御这个的考虑。', 2, 13, 0, 1),
  (127, 'synchronized 的实现原理(精简版)', NULL, 'synchronized 底层是通过对象的 Monitor 监视器实现的,同步代码块编译后是 monitorenter 和 monitorexit 指令,锁的状态信息记录在对象头的 Mark Word 里。拿不到锁的线程会被阻塞,而阻塞唤醒需要操作系统介入、有内核态切换,开销比较大,所以 JDK 6 引入了锁升级优化:先是偏向锁,只有一个线程用锁时几乎零开销;有竞争就升级成轻量级锁,靠 CAS 和自旋等待;竞争激烈再膨胀为重量级锁,线程真正阻塞。整体思路就是按竞争程度用最小的代价加锁。

(追问储备:Mark Word 四种锁状态;偏向锁 CAS 写线程 ID、JDK 15 已废弃;轻量级锁的 Lock Record 与自适应自旋;ObjectMonitor 的 owner/EntryList/WaitSet 结构,这也是 wait/notify 必须在同步块里用的原因;锁消除与锁粗化;三大语义保证:原子性、可见性、有序性;可重入、非公平、不可中断,不可中断是与 ReentrantLock 的重要区别。)', 2, 3, 0, 1),
  (128, '对 ThreadLocal 的理解', NULL, 'ThreadLocal 提供线程级别的变量隔离,每个线程读写的都是自己的独立副本。它和锁的思路相反:锁是共享资源排队访问,ThreadLocal 是干脆不共享,空间换安全。

原理上,数据其实存在线程自己身上:每个 Thread 对象内部有个 ThreadLocalMap,key 是 ThreadLocal 对象,value 是存的值,get 的时候先拿当前线程、再取它的 Map、用 ThreadLocal 自己做 key 去查,所以天然隔离。

典型场景比如 Web 项目保存登录用户上下文——我毕设里就是拦截器解析 JWT 后把用户 ID 存进 ThreadLocal,后面业务层随取随用,不用层层传参;Spring 的事务管理绑定 Connection 也是靠它。

最需要注意的是内存泄漏:Entry 对 key 是弱引用、对 value 是强引用,key 被 GC 后 value 还挂在线程上回收不掉,线程池里线程长期存活,不仅泄漏,下个任务还可能读到上个任务的脏数据。所以规范是用完必须在 finally 里 remove,比如拦截器的 afterCompletion 里清理。', 2, 3, 0, 1);

INSERT INTO question_tag (question_id, tag_id) VALUES
  (101, 8),
  (102, 8),
  (103, 14),
  (103, 8),
  (106, 8),
  (108, 8),
  (109, 2),
  (109, 8),
  (111, 7),
  (115, 14),
  (118, 8),
  (120, 4),
  (120, 8),
  (121, 5),
  (121, 8),
  (123, 8),
  (125, 7),
  (126, 15),
  (126, 8),
  (127, 2),
  (127, 8),
  (128, 8);

SELECT 'imported_questions' AS item, COUNT(*) AS cnt FROM question WHERE id BETWEEN 101 AND 128
UNION ALL SELECT 'imported_tags', COUNT(*) FROM question_tag WHERE question_id BETWEEN 101 AND 128;
