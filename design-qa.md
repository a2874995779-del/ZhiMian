# 智面前端视觉验收

## 对照对象

- 源视觉目标：C:/Users/acer/AppData/Local/Temp/codex-clipboard-d95136fb-8976-481b-ab4b-a5eae8b3b975.png
- 实现地址：http://127.0.0.1:5173/interview?sessionId=20
- 实现截图：Codex In-app Browser，Tab 4，浏览器截图已在本次验收中捕获
- 源图像素：1586 x 991
- 实现截图像素：1280 x 720，IAB 固定浏览器视口
- 状态：已登录、Java 并发面试进行中、首题未回答
- 密度：浏览器截图按 CSS 像素展示，未做设备像素比放大

源图与实现截图的视口高度不同，因此本次以布局比例、组件关系、色彩和交互状态为主要验收依据，不把内容条数差异误判为视觉缺陷。

## 全局对照

目标图是一个正在进行中的面试工作区，而不是面试模式选择页。实现已通过点击专项面试方向进入真实会话，再进行同状态截图。

## 五项重点验收

### 1. 字体与排版

- 目标：系统无衬线字体、深色标题、较轻的辅助信息。
- 实现：使用 Apple 系统字体栈、石墨色标题、灰色辅助文本，标题和控件没有负字距。
- 结果：通过。

### 2. 间距与布局节奏

- 目标：约 250px 侧栏，主区域上方标题，中央对话区与右侧计划区并列。
- 实现：侧栏固定为 250px；面试状态隐藏普通页面顶部栏；工作区使用中央会话列加 330px 计划列，顶部进度条和底部输入框保持稳定尺寸。
- 结果：通过。

### 3. 颜色与视觉令牌

- 目标：白色表面、浅灰背景、石墨文字、单一蓝色强调，阴影轻。
- 实现：统一使用 --zm-* 令牌；移除背景光斑、过度玻璃拟态和多色渐变；按钮、选中进度节点、活动计划均使用系统蓝。
- 结果：通过。

### 4. 图片与图标资产

- 目标：目标图使用真实 AI 面试官头像和清晰的导航/状态图标。
- 实现：新增 frontend/public/ai-interviewer.png 作为 AI 面试官头像；导航和操作图标继续使用 Element Plus 图标库；品牌图标复用项目已有 favicon 资源并放入蓝色品牌容器。
- 结果：通过。品牌符号形状与源图不完全相同，但不影响信息识别和功能。

### 5. 文案与内容

- 目标：面试标题、进度、对话、发送区、右侧面试计划均为原生可交互文本。
- 实现：标题使用当前真实面试方向；题目内容来自后端会话；面试进度和今日进度来自真实接口；计划状态随回答数量计算。
- 结果：通过。目标图中的示例回答数量与当前新会话不同，这是状态差异，不是静态占位。

## 交互验证

1. 访问 /interview，确认专项面试和场景面试模式选择可用。
2. 点击 Java 并发方向，成功创建会话并进入 /interview?sessionId=20。
3. 确认面试状态下普通应用顶部栏被替换为面试标题栏。
4. 确认结束按钮、回答输入框、发送按钮、进度区域和右侧计划区域均为真实控件。
5. 确认左侧今日进度从仪表盘接口加载，而不是写死数字。

## 迭代记录

### 第一次对照

- 发现：旧实现是浮动侧栏，面试页采用考官卡片加聊天卡片的两栏布局，没有右侧面试计划。
- 修复：改为固定 250px 侧栏；新增面试状态专用工作区；加入横向进度、聊天区、输入区和右侧计划。

### 第二次对照

- 发现：AI 头像是 AI 文字占位；计划模块存在重复命名；侧栏缺少今日进度。
- 修复：新增 AI 面试官图片资源；整理计划模块命名；复用仪表盘接口填充今日进度。

### 当前结果

- 没有发现需要阻止交付的 P0、P1 或 P2 视觉问题。
- 有一个 P3 级差异：品牌符号不是源图中的同款 Z 字标记，而是项目已有 favicon 的白色化版本。后续如有正式品牌资产，可替换 AppShell.vue 中的 brand-logo。
- 源图展示的是已进行到第 2 题并包含多轮回答的状态；当前验收截图是新会话首题状态，属于真实数据状态差异。

## 构建与服务

- npm.cmd run build：通过。
- 前端服务：http://127.0.0.1:5173/，已启动并保持运行。
- 构建仍有一个既有的主 JS 包超过 500 kB 的提示，不影响本次视觉和功能验收。

final result: passed
