# 任务 1.4 · 登出与 token 黑名单

> 对应开发文档第 5 节「任务 1.4」。请**亲手把代码敲进项目**,敲完发我 review。

---

## ⚠️ 前置检查

今天要在 `UserService` 接口里新增 `logout()` 方法,顺手把任务 1.3 里提过、一直没处理的包结构问题解决掉,不然新代码也要跟着错下去:

1. `UserService.java` 现在 `package com.zhimian.service.impl;`,应为 `com.zhimian.service`。
2. `UserServiceImpl.java` 现在 `package com.zhimian.service.impl.service.impl;`(IDE 自动生成实现类时误触发的多层嵌套),应为 `com.zhimian.service.impl`。
   用 IDE 的 "Move Class" 重构改这两个文件,不要手动剪切粘贴——IDE 会自动帮你把所有 import 它们的地方一起改掉。改完确认项目还能编译。
3. `UserService` 接口里 `register`/`login` 的参数上写了 `@Valid`,这个注解在接口方法上不生效(校验只在 `@RequestBody` 入口触发),顺手删掉。
4. `AuthController.java` 顶部有一行没用到的 `import org.apache.catalina.User;`(和项目里的 `model.entity.User`毫无关系,是 IDE 自动导入时选错了包),今天要改这个文件,顺手删掉。

这四项修完再往下写。

---

## 一、本任务要实现什么

一句话:JWT 一旦签发,靠拦截器本地验签是没法让某一个 token"提前作废"的(密钥没变,签名永远有效)。解决办法是维护一个"黑名单"——登出时把这个 token 的唯一标识(`jti`)记进 Redis,并让这条记录在 token 本该过期的时间自动消失;拦截器验签通过后,多查一次"这个 jti 是否在黑名单里"。

链路:`AuthController.logout() → UserService.logout()(取出当前 token 的 jti 和剩余有效期,写入 Redis)`;`JwtInterceptor.preHandle 验签通过后 → 查 Redis 黑名单 → 命中则视为"已过期"`。

**关键设计点**:`jti` 和过期时间不需要在 `logout()` 里重新解析 token 拿——拦截器在这次请求进来时**已经验签解析过一次**了,没必要在 Service 层再解析一遍浪费一次 CPU。所以今天顺手把 `UserContext` 也扩展一下,让拦截器把 `jti` 和过期时间也存进去,Service 层直接读。

---

## 二、本任务要动的文件

| 操作 | 文件 | 作用 |
|------|------|------|
| 修改 | `util/JwtUtil.java` | 新增黑名单 key 前缀常量 |
| 修改 | `common/UserContext.java` | 扩展存储 `jti`、token 过期时间 |
| 修改 | `interceptor/JwtInterceptor.java` | 验签后查黑名单;把 `jti`/过期时间存入 `UserContext` |
| 修改 | `service/UserService.java` + 实现类 | 新增 `logout()` |
| 修改 | `controller/AuthController.java` | 新增 `/api/auth/logout` |

---

## 三、分步实现

### Step 1:黑名单 key 前缀 `util/JwtUtil.java`

只新增一个常量,其余不变:

```java
package com.zhimian.util;

import com.zhimian.config.JwtProperties;
import io.jsonwebtoken.Claims;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.security.Keys;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Component;

import javax.crypto.SecretKey;
import java.time.Instant;
import java.time.temporal.ChronoUnit;
import java.util.Date;
import java.util.UUID;

@Component
@RequiredArgsConstructor
public class JwtUtil {

    /** Redis 黑名单 key 前缀。拦截器(查)和 Service(写)两处都要用,放这里统一管理,不要两边各写一份字符串 */
    public static final String BLACKLIST_KEY_PREFIX = "zhimian:jwt:blacklist:";

    private final JwtProperties jwtProperties;

    public String generateToken(Long userId, String role) {
        SecretKey key = Keys.hmacShaKeyFor(jwtProperties.getSecret().getBytes());
        Instant now = Instant.now();
        Instant expireAt = now.plus(jwtProperties.getExpireHours(), ChronoUnit.HOURS);

        return Jwts.builder()
                .id(UUID.randomUUID().toString())
                .subject(String.valueOf(userId))
                .claim("role", role)
                .issuedAt(Date.from(now))
                .expiration(Date.from(expireAt))
                .signWith(key)
                .compact();
    }

    public Claims parseToken(String token) {
        SecretKey key = Keys.hmacShaKeyFor(jwtProperties.getSecret().getBytes());
        return Jwts.parser()
                .verifyWith(key)
                .build()
                .parseSignedClaims(token)
                .getPayload();	
    }
}
```

【讲解】

- 任务 1.2 写 `generateToken` 时就已经埋了 `.id(UUID.randomUUID().toString())` 这行,当时注释写的是"jti:唯一标识,登出黑名单(任务 1.4)靠它"——今天这个伏笔正式派上用场。`jti` 是 JWT 标准里专门为"唯一标识这一个 token"设计的字段,比拿整个 token 字符串当 key 更短、更规范。
- key 前缀常量放 `JwtUtil` 而不是各写各的字符串:两处(拦截器查、Service 写)如果各写一份 `"zhimian:jwt:blacklist:"`,以后要改前缀就要改两个地方还容易漏改一处——这是最基本的"不要重复自己"(DRY),不算过度设计。

### Step 2:扩展 `common/UserContext.java`

```java
package com.zhimian.common;

/**
 * 保存当前请求的登录用户信息,基于 ThreadLocal 实现"线程内全局变量"
 * 必须在请求处理完成后调用 remove(),否则线程池复用线程时会导致用户信息串号
 */
public class UserContext {

    private static final ThreadLocal<LoginUser> CONTEXT = new ThreadLocal<>();

    public static void set(Long userId, String role, String jti, long expireAt) {
        CONTEXT.set(new LoginUser(userId, role, jti, expireAt));
    }

    public static Long getUserId() {
        LoginUser user = CONTEXT.get();
        return user == null ? null : user.userId();
    }

    public static String getRole() {
        LoginUser user = CONTEXT.get();
        return user == null ? null : user.role();
    }

    public static String getJti() {
        LoginUser user = CONTEXT.get();
        return user == null ? null : user.jti();
    }

    public static long getExpireAt() {
        LoginUser user = CONTEXT.get();
        return user == null ? 0 : user.expireAt();
    }

    public static void remove() {
        CONTEXT.remove();
    }

    private record LoginUser(Long userId, String role, String jti, long expireAt) {
    }
}
```

【讲解】

- `expireAt` 用 `long`(epoch 毫秒,来自 `claims.getExpiration().getTime()`)而不是 `Date`:`record` 里放可变对象类型(`Date` 是可变的)会破坏 record 本该有的不可变语义,存基础类型更干净。
- `set()` 方法签名从两个参数变成四个,调用它的地方(`JwtInterceptor`)要跟着改——这是"改一处、影响另一处"的典型例子,编译器会在你漏改的地方直接报错,顺着报错改就行。

### Step 3:拦截器查黑名单 + 写入 jti `interceptor/JwtInterceptor.java`

```java
package com.zhimian.interceptor;

import com.zhimian.annotation.RequireAdmin;
import com.zhimian.common.ErrorCode;
import com.zhimian.common.UserContext;
import com.zhimian.exception.BusinessException;
import com.zhimian.util.JwtUtil;
import io.jsonwebtoken.Claims;
import io.jsonwebtoken.ExpiredJwtException;
import io.jsonwebtoken.JwtException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.stereotype.Component;
import org.springframework.util.StringUtils;
import org.springframework.web.method.HandlerMethod;
import org.springframework.web.servlet.HandlerInterceptor;

@Component
@RequiredArgsConstructor
public class JwtInterceptor implements HandlerInterceptor {

    private static final String TOKEN_PREFIX = "Bearer ";

    private final JwtUtil jwtUtil;
    private final StringRedisTemplate redisTemplate;

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        if (!(handler instanceof HandlerMethod handlerMethod)) {
            return true;
        }

        String authHeader = request.getHeader("Authorization");
        if (!StringUtils.hasText(authHeader) || !authHeader.startsWith(TOKEN_PREFIX)) {
            throw new BusinessException(ErrorCode.NOT_LOGIN);
        }
        String token = authHeader.substring(TOKEN_PREFIX.length());

        Claims claims;
        try {
            claims = jwtUtil.parseToken(token);
        } catch (ExpiredJwtException e) {
            throw new BusinessException(ErrorCode.TOKEN_EXPIRED);
        } catch (JwtException | IllegalArgumentException e) {
            throw new BusinessException(ErrorCode.NOT_LOGIN);
        }

        // 验签只能证明"token 是我签的、没被改过、没过期",证明不了"我有没有主动作废它"
        // 所以验签通过之后还要多查一次黑名单——这次查询就是"无状态"为了"能登出"付出的代价
        String jti = claims.getId();
        if (Boolean.TRUE.equals(redisTemplate.hasKey(JwtUtil.BLACKLIST_KEY_PREFIX + jti))) {
            throw new BusinessException(ErrorCode.TOKEN_EXPIRED);
        }

        Long userId = Long.valueOf(claims.getSubject());
        String role = claims.get("role", String.class);
        UserContext.set(userId, role, jti, claims.getExpiration().getTime());

        boolean requireAdmin = handlerMethod.hasMethodAnnotation(RequireAdmin.class);
        if (requireAdmin && !"admin".equals(role)) {
            UserContext.remove();
            throw new BusinessException(ErrorCode.NO_AUTH);
        }
        return true;
    }

    @Override
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex) {
        UserContext.remove();
    }
}
```

【讲解】

- 黑名单命中时返回的错误码是 `TOKEN_EXPIRED`(40101)而不是新建一个"已登出"的错误码:对客户端来说,"token 过期"和"token 已被登出"要做的事完全一样——清掉本地 token、跳转登录页,没必要为了区分内部原因新增一个 40102,徒增复杂度(开发文档 4.2 节的错误码规范里,40101 的典型场景本来就写了"token 过期或已登出")。
- `redisTemplate.hasKey(...)` 返回的是 `Boolean`(可能为 `null`),不能直接 `if (redisTemplate.hasKey(...))`——虽然 Lettuce 驱动实际不会返回 null,但方法签名上是 `Boolean` 包装类型,直接拆箱在类型层面就是有风险的写法,`Boolean.TRUE.equals(...)` 是更稳妥的判断方式,不会有拆箱空指针的隐患。
- 这一步的查询开销:黑名单里的 key 数量等于"最近一段时间内主动登出的用户数",远小于"当前所有登录用户数",Redis 单次 `EXISTS` 是 O(1),这次多出来的查询成本很低——但"低"不代表"没有",这正是下面要回答的面试题的核心。

### Step 4:Service 新增 `logout()`

`service/UserService.java`:

```java
package com.zhimian.service;

import com.zhimian.model.dto.UserLoginDTO;
import com.zhimian.model.dto.UserRegisterDTO;
import com.zhimian.model.vo.LoginVO;

public interface UserService {

    Long register(UserRegisterDTO dto);

    LoginVO login(UserLoginDTO dto);

    void logout();
}
```

`service/impl/UserServiceImpl.java`(`register`/`login` 内容不变,完整贴出方便对照包名和新增部分):

```java
package com.zhimian.service.impl;

import com.zhimian.common.ErrorCode;
import com.zhimian.common.UserContext;
import com.zhimian.exception.BusinessException;
import com.zhimian.mapper.UserMapper;
import com.zhimian.model.dto.UserLoginDTO;
import com.zhimian.model.dto.UserRegisterDTO;
import com.zhimian.model.entity.User;
import com.zhimian.model.vo.LoginVO;
import com.zhimian.model.vo.UserVO;
import com.zhimian.service.UserService;
import com.zhimian.util.JwtUtil;
import lombok.RequiredArgsConstructor;
import org.springframework.beans.BeanUtils;
import org.springframework.dao.DuplicateKeyException;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.stereotype.Service;
import org.springframework.util.StringUtils;

import java.util.concurrent.TimeUnit;

@Service
@RequiredArgsConstructor
public class UserServiceImpl implements UserService {

    private final UserMapper userMapper;
    private final BCryptPasswordEncoder passwordEncoder;
    private final JwtUtil jwtUtil;
    private final StringRedisTemplate redisTemplate;

    @Override
    public Long register(UserRegisterDTO dto) {
        User user = new User();
        user.setUsername(dto.getUsername());
        user.setPassword(passwordEncoder.encode(dto.getPassword()));
        user.setNickname(StringUtils.hasText(dto.getNickname()) ? dto.getNickname() : dto.getUsername());
        user.setRole("user");

        try {
            userMapper.insert(user);
        } catch (DuplicateKeyException e) {
            throw new BusinessException(ErrorCode.CONFLICT, "用户名已存在");
        }
        return user.getId();
    }

    @Override
    public LoginVO login(UserLoginDTO dto) {
        User user = userMapper.selectByUsername(dto.getUsername());
        if (user == null || !passwordEncoder.matches(dto.getPassword(), user.getPassword())) {
            throw new BusinessException(ErrorCode.NOT_LOGIN, "用户名或密码错误");
        }

        String token = jwtUtil.generateToken(user.getId(), user.getRole());

        UserVO userVO = new UserVO();
        BeanUtils.copyProperties(user, userVO);

        LoginVO loginVO = new LoginVO();
        loginVO.setToken(token);
        loginVO.setUser(userVO);
        return loginVO;
    }

    @Override
    public void logout() {
        String jti = UserContext.getJti();
        long expireAt = UserContext.getExpireAt();
        long remainingSeconds = (expireAt - System.currentTimeMillis()) / 1000;

        // 理论上走到这里 remainingSeconds 必然 > 0(拦截器已经验证过 token 没过期才会放行到这)
        // 但如果 token 恰好在这几毫秒内跨越过期边界,SETEX 一个 <= 0 的 TTL 会直接抛异常,加这个判断兜底
        if (remainingSeconds > 0) {
            redisTemplate.opsForValue().set(
                    JwtUtil.BLACKLIST_KEY_PREFIX + jti,
                    "1",
                    remainingSeconds,
                    TimeUnit.SECONDS
            );
        }
    }
}
```

【讲解】

- **为什么 TTL 必须设成"token 剩余有效期"而不是固定值(比如固定写 24 小时)**:黑名单的唯一使命是"在 token 本该失效的那一刻之前,记得它已经作废"。固定值如果比剩余有效期短,token 还没到期黑名单先消失了,等于白登出;如果固定值偏长(比如不管 token 还剩多久,统一存 24 小时),token 明明已经自然过期了,黑名单记录还占着 Redis 内存不放,越攒越多——这是开发文档里明确点出的"易踩的坑"。用剩余有效期做 TTL,黑名单的生命周期和 token 的生命周期严格对齐,过期自动清,不需要任何定时清理任务。
- `redisTemplate.opsForValue().set(key, "1", ttl, unit)`:value 具体存什么无所谓(这里只关心 key 存不存在),存 `"1"` 只是图省事,不需要设计 value 结构。
- `logout()` 不需要参数、不需要查数据库:要作废的就是"这次请求自己带的这个 token",信息已经在 `UserContext` 里了(上一步拦截器验签时顺手存的),不需要 Controller 再把 token 或 userId 传进来。

### Step 5:Controller 新增 `/logout`

`controller/AuthController.java`:

```java
package com.zhimian.controller;

import com.zhimian.common.Result;
import com.zhimian.model.dto.UserLoginDTO;
import com.zhimian.model.dto.UserRegisterDTO;
import com.zhimian.model.vo.LoginVO;
import com.zhimian.service.UserService;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/auth")
@RequiredArgsConstructor
public class AuthController {

    private final UserService userService;

    @PostMapping("/register")
    public Result<Long> register(@Valid @RequestBody UserRegisterDTO dto) {
        return Result.success(userService.register(dto));
    }

    @PostMapping("/login")
    public Result<LoginVO> login(@Valid @RequestBody UserLoginDTO dto) {
        return Result.success(userService.login(dto));
    }

    @PostMapping("/logout")
    public Result<Void> logout() {
        userService.logout();
        return Result.success();
    }
}
```

【讲解】

- `/api/auth/logout` 不需要加到 `WebConfig` 的白名单里——它本来就应该受拦截器保护(必须带合法 token 才能登出),现状已经是这样,不用改 `WebConfig`。
- 没有请求体,也没有从 header 里手动再取一次 token——都是 Step 4 里说的原因,信息已经在 `UserContext` 里了。

---

## 四、自测清单

```bash
# 0. 注册并登录拿到 token
curl -s -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"logouttest","password":"abc12345"}'

TOKEN=$(curl -s -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"logouttest","password":"abc12345"}' | grep -o '"token":"[^"]*"' | cut -d'"' -f4)
echo "TOKEN=$TOKEN"

# 1. 正常登出 → 期望 code=0
curl -s -X POST http://localhost:8080/api/auth/logout -H "Authorization: Bearer $TOKEN"

# 2. 用同一个 token 再登出一次 → 期望 code=40101(已经进黑名单,验签能过但被拦下)
curl -s -X POST http://localhost:8080/api/auth/logout -H "Authorization: Bearer $TOKEN"
```

​	再做两项人工检查(用 `redis-cli`):

3. **黑名单确实写入了**:
   ```bash
   redis-cli KEYS "zhimian:jwt:blacklist:*"
   ```
   应该能看到一条 key(如果你反复测试过很多次,可能有多条,找最新那条)。

4. **TTL 和 token 剩余有效期对得上、且会自动消失**:
   ```bash
   redis-cli TTL "zhimian:jwt:blacklist:{上一步查到的具体jti}"
   ```
   期望返回一个接近 `expire-hours * 3600` 的正整数(减去你测试花的时间)。如果想验证"自动消失",可以临时把 `application-local.yml` 里的 `zhimian.jwt.expire-hours` 改成一个几秒能过期的值(单位是小时,可以先临时改代码里 `plus()` 的时间单位测,测完记得都改回来),重新注册登录登出一次,等它过期后再查 `TTL`,期望返回 `-2`(key 不存在)。

---

## 五、写完之后

发我代码,我会重点看:`remainingSeconds <= 0` 的兜底判断有没有漏、`UserContext` 扩展之后所有调用 `set()` 的地方是否都改了参数、黑名单 key 前缀是不是真的只在 `JwtUtil` 里定义了一份。review 通过后我会问你几个问题(预告):

1. JWT 本身是"无状态"的,为什么还要靠一个有状态的黑名单才能实现登出?这算不算违背了 JWT 的设计初衷?你怎么看待这个权衡?
2. 除了黑名单,你还能说出别的"让 token 提前失效"的方案吗?(提示:短 token + refresh token、给用户加一个"token 版本号"字段)分别说说它们和黑名单方案比,优缺点是什么。
3. 如果黑名单的 TTL 写死成固定 24 小时会出什么问题?两种反例分别说说(TTL 比剩余有效期短 / 比剩余有效期长)。
4. Redis 的 key 过期是"到点立刻物理删除"吗?如果不是,它是怎么保证内存不会被过期 key 占满的?(提示:惰性删除 + 定期删除)
